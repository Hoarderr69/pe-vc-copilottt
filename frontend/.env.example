# URL of the backend API this SPA talks to. Point this at your sandbox's
# backend deployment (or http://localhost:8000 for local dev).
# Note: frontend/.env currently in this repo points at the original deployed
# demo backend — override it for your own sandbox.
VITE_API_BASE_URL=http://localhost:8000
